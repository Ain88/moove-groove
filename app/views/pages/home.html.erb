<%= alert %>
<%= notice %>
<h2 style="font-weight: bold">Greetings,</h2>
<% if user_signed_in? %>
  <div>
    Id : <%= current_user.email %><br/>
    User No. : <%= current_user.id %>
  </div>
<% else %>
  Main page
